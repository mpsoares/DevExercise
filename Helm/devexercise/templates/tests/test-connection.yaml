apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devexercise.fullname" . }}-test-connection"
  labels:
    {{- include "devexercise.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devexercise.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
